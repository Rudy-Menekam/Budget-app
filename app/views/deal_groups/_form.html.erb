<%= form_with(model: deal_group) do |form| %>
  <% if deal_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(deal_group.errors.count, "error") %> prohibited this deal_group from being saved:</h2>

      <ul>
        <% deal_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :deal_id, style: "display: block" %>
    <%= form.text_field :deal_id %>
  </div>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.text_field :group_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
